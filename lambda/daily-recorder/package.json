{
  "name": "daily-recorder",
  "version": "1.0.0",
  "description": "Daily learning record generator",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "node dist/index.js",
    "clean": "rm -rf dist",
    "deploy": "npm run build && cd dist && zip -r ../daily-recorder.zip . && cd .."
  },
  "keywords": [
    "lambda",
    "github",
    "daily-record",
    "notion"
  ],
  "author": "Gaku52",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@notionhq/client": "^5.4.0",
    "@octokit/rest": "^22.0.1",
    "@supabase/supabase-js": "^2.82.0",
    "@types/node": "^24.10.1",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.158"
  }
}
